const fs = require("node:fs");
const http = require("node:http");
const {METHODS,} = http;
const {Worker} = require("node:cluster")
const {access, constants, } = fs;
Worker. 


const num = 0o2343;
console.log(num.toString(10));





access("./reverseArray.js", constants.F_OK, (err) => console.log(err));
